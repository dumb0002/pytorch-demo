apiVersion: networking.istio.io/v1alpha3
kind: EnvoyFilter
metadata:
  name: granite-3-2b-activator
spec:
  configPatches:
  - applyTo: HTTP_ROUTE
    match:
      routeConfiguration:
        vhost:
          name: "*:80"
          route:
            name: sza.granite-3-2b.0 # TODO: what .0?
    patch:
      operation: MERGE
      value:
        typed_per_filter_config:
          envoy.filters.http.activator.ext_proc:
            "@type": type.googleapis.com/envoy.extensions.filters.http.ext_proc.v3.ExtProcPerRoute
            overrides:
              processing_mode:
                request_header_mode: "SEND"
                response_header_mode: "SKIP"
                request_body_mode: "FULL_DUPLEX_STREAMED"
                response_body_mode: "NONE"
                request_trailer_mode: "SEND"
                response_trailer_mode: "SKIP"
              grpc_service:
                envoy_grpc:
                  cluster_name: outbound|9002||granite-3-2b-activator.pytorch-demo.svc.cluster.local
---
apiVersion: networking.istio.io/v1
kind: DestinationRule
metadata:
  name: granite-3-2b-activator
spec:
  host: granite-3-2b-activator.pytorch-demo.svc.cluster.local
  trafficPolicy:
      tls:
        mode: SIMPLE
        insecureSkipVerify: true
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: granite-3-2b-activator
spec:
  selector:
    matchLabels:
      app: granite-3-2b-activator
  template:
    metadata:
      labels:
        app: granite-3-2b-activator
    spec:
      containers:
      - name: activator
        image: kind.local/llm-d-activator/activator:dev
        imagePullPolicy: Never
        args:
        - --pool-name
        - granite-3-2b-epp
        - --pool-namespace
        - pytorch-demo
        - --pool-group
        - inference.networking.x-k8s.io
        - --zap-encoder
        - json
        - --v
        - "2"
        ports:
        - containerPort: 9002
          name: grpc
          protocol: TCP
        - containerPort: 9003
          name: grpc-health
          protocol: TCP
      serviceAccountName: default
---
apiVersion: v1
kind: Service
metadata:
  name: granite-3-2b-activator
spec:
  selector:
    app: granite-3-2b-activator
  ports:
  - name: grpc-ext-proc
    port: 9002
    protocol: TCP
    targetPort: 9002
  type: ClusterIP
