apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: granite-3-2b
spec:
  parentRefs:
  - group: gateway.networking.k8s.io
    kind: Gateway
    name: sza
  rules:
    - backendRefs:
      - group: inference.networking.x-k8s.io
        kind: InferencePool
        name: granite-3-2b-epp
        port: 8000
      filters:
      - type: URLRewrite
        urlRewrite:
          path:
            replacePrefixMatch: /
            type: ReplacePrefixMatch
      matches:
      - path:
          type: PathPrefix
          value: /granite3
      timeouts:
        request: 300s
